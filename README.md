# Ałek’ryŧhæ Core 0.2.3

Shared Windows runtime/host for `.alek` applications. Meggy is distributed as a separate application repository.

Build with .NET 10 SDK on Windows using `BUILD_CORE.cmd`. User WebView profiles, cookies, history, sessions, caches, logs and application saves are intentionally excluded.
